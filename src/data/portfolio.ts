export interface Portfolio {
  title: string;
  description: string;
  imageUrl?: string;
  projectUrl?: string;
  codeUrl?: string;
  technologies?: string[];
}

export const portfolioData: Portfolio[] = [
  // Portfolio section is empty for now, focusing on publications
];
